# 🔧 Устранение неполадок

## Ошибка: `Cannot destructure property 'auth' of 'a' as it is undefined`

Эта ошибка возникает в Payload CMS при попытке получить контекст аутентификации. Вот что нужно проверить:

### 1. Проверьте переменные окружения

Убедитесь, что в `.env` файле на сервере установлены все необходимые переменные:

```bash
# Проверьте на сервере
cat .env

# Должны быть:
DATABASE_URL=mongodb://mongo:27017/printshop
PAYLOAD_SECRET=your-secret-key-min-32-chars
NODE_ENV=production
PORT=3002
```

**Важно:** `PAYLOAD_SECRET` должен быть установлен и содержать минимум 32 символа!

### 2. Проверьте подключение к MongoDB

```bash
# Проверьте, что MongoDB контейнер работает
docker-compose ps mongo

# Проверьте логи MongoDB
docker-compose logs mongo | tail -50

# Проверьте подключение из контейнера приложения
docker exec printshop_app node -e "console.log(process.env.DATABASE_URL)"
```

### 3. Проверьте, что база данных инициализирована

```bash
# Подключитесь к MongoDB
docker exec -it printshop_mongo mongosh printshop

# Проверьте коллекции
show collections

# Должны быть коллекции: users, media, globals и т.д.
```

### 4. Проверьте логи приложения

```bash
# Полные логи
docker-compose logs printshop_app

# Последние 100 строк
docker-compose logs printshop_app | tail -100

# Логи в реальном времени
docker-compose logs -f printshop_app
```

Ищите ошибки:
- Ошибки подключения к MongoDB
- Ошибки инициализации Payload
- Ошибки при загрузке конфигурации

### 5. Пересоберите образ без кэша

```bash
# Остановите контейнеры
docker-compose down

# Удалите старые образы (опционально)
docker rmi printshop_printshop 2>/dev/null || true

# Пересоберите без кэша
docker-compose build --no-cache printshop

# Запустите
docker-compose up -d

# Проверьте логи
docker-compose logs -f printshop_app
```

### 6. Проверьте версии зависимостей

Убедитесь, что версии в `package.json` совместимы:

```json
{
  "payload": "^3.72.0",
  "@payloadcms/next": "^3.72.0",
  "@payloadcms/db-mongodb": "^3.72.0",
  "next": "^15.4.0"
}
```

### 7. Проверьте конфигурацию Payload

Убедитесь, что в `src/payload.config.ts`:
- `secret` установлен из переменной окружения
- `DATABASE_URL` правильно настроен
- Коллекция `Users` включена с `auth: true`

### 8. Альтернативное решение: Использовать локальный экземпляр

Если проблема сохраняется, попробуйте использовать локальный экземпляр Payload без контекста запроса. Это уже реализовано в `src/lib/get-payload.ts`.

### 9. Проверьте, что файлы скопированы в Docker образ

```bash
# Проверьте содержимое образа
docker run --rm printshop_printshop ls -la /app/src/lib/

# Должен быть файл get-payload.ts
```

### 10. Проверьте health check

```bash
# Проверьте доступность приложения
curl http://localhost:3002/api/health

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

## Дополнительные проверки

### Проверка сборки локально

Перед развертыванием на сервере убедитесь, что проект собирается локально:

```bash
npm run build
```

Если сборка падает локально, исправьте ошибки перед развертыванием.

### Проверка переменных окружения в контейнере

```bash
# Проверьте все переменные окружения
docker exec printshop_app env | grep -E "DATABASE_URL|PAYLOAD_SECRET|NODE_ENV"
```

### Проверка подключения к базе данных

```bash
# Попробуйте подключиться к MongoDB из контейнера приложения
docker exec printshop_app sh -c "node -e \"const mongoose = require('mongoose'); mongoose.connect(process.env.DATABASE_URL).then(() => console.log('Connected')).catch(e => console.error(e))\""
```

## Если ничего не помогает

1. **Создайте бэкап базы данных:**
   ```bash
   ./scripts/backup-mongo.sh
   ```

2. **Проверьте документацию Payload CMS:**
   - https://payloadcms.com/docs
   - https://payloadcms.com/docs/getting-started/installation

3. **Проверьте Issues на GitHub:**
   - https://github.com/payloadcms/payload/issues

4. **Соберите полную информацию для отчета:**
   ```bash
   # Версии
   docker --version
   docker-compose --version
   node --version
   
   # Логи
   docker-compose logs printshop_app > app-logs.txt
   docker-compose logs mongo > mongo-logs.txt
   
   # Конфигурация
   cat .env > env-backup.txt
   cat docker-compose.yml > docker-compose-backup.yml
   ```

## Частые проблемы и решения

### Проблема: PAYLOAD_SECRET не установлен
**Решение:** Установите `PAYLOAD_SECRET` в `.env` файле (минимум 32 символа)

### Проблема: MongoDB не подключается
**Решение:** Проверьте `DATABASE_URL` и убедитесь, что MongoDB контейнер запущен

### Проблема: Ошибка при сборке
**Решение:** Проверьте, что все зависимости установлены и версии совместимы

### Проблема: Ошибка в production, но работает локально
**Решение:** Убедитесь, что все переменные окружения установлены на сервере

